1. How would you define machine learning
Machine learning is the art of programming computers so they can learn from data

2. Can you name four types of applications where it shines
in applications with no known algorithmic solution, or in areas with too many hand tuned solutions, helping humans learn, fluctuating environments

3. what is a labeled training set
Labeled training sets are sets of data with their desired label already attached to it

4. What are the two most common supervised tasks?
Classification and logistic regression

5. Can you name four uncommon supervised tasks


6. What type of alogriithm would you use to allow a robot to walk in various unknown terriains?
Reinforcement learning

7. What type of algorithm would you use to segment your customers into multiple groups?
    unsupervised learning custering algorithm

8. Would you frame the problem of spam detection as a supevised learning problem or an unsupervised learning problem?
    typically supervised learning but is possible with unsupervised but it may not work as intended as it might block items it shouldnt

9. What is an online learning system
    an online learning system is a system that continually learns on the data it is provided after it is published to production

10. What is out of core learning
    out of core learning is done in certain situations when all of the training data cannot fit onto the main memory of the model's machine. Instead it is split up into chunks and fed incrementally to the model to train on.

11. What type of algorithm relies on a similarity measure to make predictions?
    Instance based learning

12. What is the difference between a model parameter and a model hyperparameter
    a model parameter is what is used for the model to predict new instances. A HyperParameter is part of the learning model itself and is independent of the model parameters and data

13. What do model based algorithms search for? What is the most common strategy they use to succeed? How do they make predictions
    

14. What are the four main challenges of machine learning

15. If your model performs great on the training data buut generalizes poorly to new instances, what is happening? Can you name three possible solutions

16. What is a test set, and why would you want to use it?

17. What is the purpose of a validation set?